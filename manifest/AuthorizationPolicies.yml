apiVersion: "security.istio.io/v1beta1"
kind: "AuthorizationPolicy"
metadata:
  name: "demoapp"
  namespace: demo-service
spec:
  selector:
    matchLabels:
      app: demoapp
  rules:
  - to:
    - operation:
        methods: ["GET"]
